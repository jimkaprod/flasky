virtual env
$sudo apt-get install python3-venv
ex : $python3 -m venv virtual-environment-name
$python3 -m venv venv
Activate
$source venv/bin/activate
Desactiver:
$deactivate
Pip generer requirements.txt
$pip freeze >requirements.txt
Reinstal
$pip install -r requirements.txt

MIGRATION SCRIPT (ALEMBIC)
- Mettre à jour le model
- $flask db migrate
- voir le script générer pour voir les changements, en cas de suppression d'une colonne attention à la perte des données
- $flask db upgrade


